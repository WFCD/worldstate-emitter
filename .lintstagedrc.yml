"*.{ts,js}":
  - biome check --write --no-errors-on-unmatched
package*.json:
  - biome check --write --no-errors-on-unmatched
  - npm dedupe
  - npx sort-package-json
"*.{json,yml,yaml}":
  - biome check --write --no-errors-on-unmatched
